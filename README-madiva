Initialise everthing Bluetooth related
Check script *** BT-Initialize interface.sh**** execute it when system initializes

To see properties of your target device
#sdptool browse <TARGET DEVICE ID>(TARGET)

connecting to a bluetooth device cause Bluetoothctl connects & disconnects
#rfcomm connect hci0(HOST) <TARGET DEVICE ID>(TARGET) 2(CHANNEL)

